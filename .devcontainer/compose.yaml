services:
  utakata_marp_devcontainer:
    build: 
      context: .
      dockerfile: Dockerfile
    image: utakata_marp_devcontainer_image
    container_name: utakata_marp_devcontainer_container
    volumes:
      - ../workspace:/workspace
    working_dir: /workspace
    tty: true
    user: utakata